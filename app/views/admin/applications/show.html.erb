<h1><%= @application.name %>'s Application</h1>

<h3>Pending Pets:</h3>
<ul>
  <% @application.pets.each do |pet| %>
    <li><%= pet.name %> <%= button_to "Approve #{pet.name}", "/admin/applications/#{@application.id}" %></li>
  <% end %>
</ul>