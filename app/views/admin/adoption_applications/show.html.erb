<h3>Admin Shelters Show Page</h3>
<p>Application ID: <%= @adoption_app.id %></p>

<h1>Pets Associated with <%= @adoption_app.id %></h1>
<% if @adoption_app.pets.nil? %>
    <p>No pets found.</p>
<% else %>
    <ol>
    <% @adoption_app.pets.each do |pet| %>
        <li><%= pet.name %></li>
        <ul>
            <li>Breed: <%= pet.breed %></li>
            <li>Age: <%= pet.age %></li>
            <li>Adoptable: <%= pet.adoptable %></li>
        </ul>
        <% if pet.adoptable = true %>
            <%= form_with url: "/admin/applications/#{@adoption_app.id}", method: :patch do |form| %>
                <%= form.submit "Approve" %>
            <% end %>
        <% else %>
            <p>This pet has been approved.</p>
        <% end %>
    <% end %>
    </ol>
<% end %>
