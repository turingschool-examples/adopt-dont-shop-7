<h1><%= @applicant.name %></h1>
<p>Street Address: <%= @applicant.street_address %></p>
<p>City: <%= @applicant.city %></p>
<p>State: <%= @applicant.state %></p>
<p>Zip Code: <%= @applicant.zip_code %></p>
<p>Description: <%= @applicant.description %></p>
<p>Status: <%= @applicant.status %></p>
<div class="search">
  <%= form_with(url: applicant_path(@applicant), local: true, method: :get) do |f| %>
    <h3> Add a Pet to this Application </h3>
    <p>Pet: <% f.label :pet_name %></p>
    <p><%= f.text_field :pet_name %></p>
    <p><%= f.submit "submit" %></p>
  <% end %>
</div>
<% @matching_pets.each do |pet| %>
  <p> name: <%= pet.name %> </p>
  <% if pet.adoptable? %>
    <% if @applicant.pets.exists?(pet.id) %>
      <p>Pet is already associated with the applicant.</p>
    <% else %>
      <%= button_to 'Add Pet', create_applicant_pet_path(applicant_id: @applicant.id, pet_id: pet.id), method: :post %>
    <% end %>
  <% end %>
<% end %>
<h3>Associated Pets</h3>
<% @associated_pets.each do |pet| %>
  <p>Pet Name: <%= link_to pet.name, pet_path(pet) %></p>
<% end %>
<div "adoption description">
  <%= form_with(url: applicant_path(@applicant), method: :patch) do |f| %>
    <h3>Submit My Application </h3>
    <p>Description: <% f.label :description %></p>
    <p><%= f.text_area :adoption_description %></p>
    <p><%= f.submit "submit description" %></p>
  <% end %>
</div>
